# 发布前检查（开发文档 15.5）

检查日期：2026-09-26　检查人：项目维护者（自动化辅助）

| 项 | 结论 | 说明 |
|---|---|---|
| PyPI 包名 `knot` | **已被占用** | 存在他人项目 `knot`（dependency container，2016 年最后发布） |
| PyPI 包名 `knot-ledger` | **可用** | 查询 `https://pypi.org/pypi/knot-ledger/json` 返回 404（未被占用） |
| GitHub 仓库名 `knot` | 已被占用 | 社区存在同名仓库；本项目使用 `koltfranz/Knot`，无冲突 |
| 商标「结绳」第 9 类 / 第 42 类 | **待人工核实** | 自动化无法可靠查询商标局数据库，需人工检索后再决定是否注册 |

## 发行名决定

- **发行名（distribution name）**：`knot-ledger`（pyproject `[project] name`）
- **导入包名**：`knot`（`import knot` 不变）
- **命令行**：`knot`（`[project.scripts]` 不变）
- **单文件程序**：`knot.pyz`

`src/knot/version.py` 会依次尝试读取 `knot-ledger` 与 `knot` 两个发行名的元数据，因此本地
`pip install -e .` 与未来从 PyPI 安装都能正确显示版本号。

## 待办

- 商标检索（第 9 类：计算机软件；第 42 类：软件设计开发）—— 不影响 0.x 阶段的源码分发与自用
- PyPI 首次发布需项目所有者授权并完成双因素认证（本项目尚未发布至 PyPI）
