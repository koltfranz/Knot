# 变更日志

本项目所有值得注意的变更都将记录于此。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### Added

- 一键启动器：`界面.bat` / `界面.sh`（进入 TUI）、`网页.bat` / `网页.sh`（启动本地服务并自动打开浏览器）
- `服务` 命令新增 `--打开浏览器` / `--不打开浏览器`：默认在交互式终端下自动打开浏览器；带口令时口令只随打开请求传给浏览器，不打印到终端
- `菜单` 新增「浏览器界面（本地服务）」「终端界面（TUI）」两项

### Fixed

- Windows 启动器只使用 ASCII 命令别名（`tui`、`serve --open`）：批处理中的中文参数会按控制台代码页解析，在非 UTF-8 控制台下会被损坏（如 `服务` → `鏈嶅姟`）；新增测试断言启动器命令行不含中文

### Notes

- 版本号待定：规划文档此前上限为 v0.7.0（稳定化），本条目对应一次功能新增，发布前需确认版本号

## [0.7.0] - 2026-09-26

稳定化发布：语法冻结、分发就绪、性能验收、缺陷清理。

### Added

- 语法冻结：`schema/GRAMMAR.md` 声明冻结（既有写法不得移除或改变语义），手册、语法大全、语法速查同步标注
- 分发：`Dockerfile`（家庭共享，账本挂载为卷）、`scripts/构建可执行.py`（Nuitka 单文件）、CI 工作流（三平台 × Python 3.11/3.13 测试矩阵与 zipapp 冒烟）
- 性能：`scripts/性能基准.py` 与 `tests/test_perf.py`（性能门槛断言）；实测 30,000 笔解析校验 1.67 s、100,000 笔 6.17 s，报表 + SQL 聚合均在 1 s 内
- 发行名改为 `knot-ledger`（`knot` 在 PyPI 已被占用），`version.py` 依次尝试两个发行名读取版本
- `docs/发布前检查.md`：记录 PyPI 包名可用性、GitHub 仓库名与商标待办
- 用户手册新增第 14 章「分发、部署与性能」

### Fixed

- **P0**：`对账 --标记` 与批量归类会改写由 `recur` 模板展开的交易（多笔展开共享同一源行区间），造成块重叠与账本损坏。现在定期展开的交易不参与对账标记与归类改写，同一源行区间只处理一次；`apply_edits` 增加重叠编辑防护（按行号降序时跳过相交区间）
- `对账` 报告新增「定期展开 N 笔（不参与标记）」提示

### Changed

- 定期展开交易的标识统一为 `core.recur.is_generated()`，供对账、归类等写路径复用
- 测试总数 237 项（新增缺陷回归 5 项与性能门槛 1 项）

### Notes

- 0.7.0 是版本规划中的稳定化版本；**`1.0.0` 及以上的版本号必须经项目所有者明确许可后方可规划**
- PyPI 尚未发布：需所有者授权与双因素认证；Docker 镜像未在本机验证（开发机未安装 Docker）

## [0.6.0] - 2026-09-26

对应里程碑 M4 后半：投资与多币种。

### Added

- 库存与成本基础：`core/inventory.py` 支持 **FIFO（默认）** 与 **移动平均**，逐批消耗、已实现与未实现盈亏、最新报价（`price` 指令，可限定日期）
- `持仓`（`holdings`）命令：数量/成本/单位成本/最新价/市值/未实现/已实现，支持 `--方法 平均`、`--折算 币种` 与 `--json`
- 多币种折算：直接报价 → 反向报价 → 经记账币种中转；缺报价单独列出，不猜测
- SQL 子集：`core/sql.py`（`SELECT … FROM 分录|流水 [WHERE …] [GROUP BY …] [ORDER BY …] [LIMIT n]`），条件运算符含 `包含/开头于/结尾于`，聚合含 `合计/计数/平均/最大/最小`；`查 --sql` 直接可用
- 现金流瀑布：`ChartSpec` 新增 `waterfall`（期初 → 经营 → 投资 → 筹资 → 期末），CLI `图 现金流` 与 Web 仪表盘共用
- Web：`/api/持仓`、`/api/图表/现金流`，仪表盘新增现金流瀑布面板
- 测试：FIFO/平均成本、报价与折算、缺报价、SQL 解析与聚合、持仓与 SQL 命令共 14 项

### Changed

- 用户手册新增第 13 章「投资与多币种」；语法大全补充 `持仓`/`tui`/`budget`/`reconcile`/`recur` 命令与瀑布、仪表盘图表

## [0.5.0] - 2026-09-26

对应里程碑 M4 前半：记账增强。

### Added

- 预算：`core/budget.py` 预算 vs 实际聚合（默认取最近有交易的月份），`预算`（`budget`）命令输出预算/实际/剩余/进度与超支标记，支持 `--月` 与 `--json`
- 预算仪表盘：`ChartSpec` 新增 `gauge` 类型（`spec_budget_gauge` + SVG 渲染），`图 预算` 与 Web「报表 → 预算进度」共用同一结构
- 对账：`core/reconcile.py`（账实比对、待对账清单、差额判定）与 `对账`（`reconcile`）命令，支持 `--余额`、`--标记`（写 `P` 标志）、`--断言`（写入余额断言，沿用文件关键字语言）、`--json`；差额不为 0 时退出码 1
- 定期交易：`定期`（`recur`）命令列出模板（周期/说明/起止/下次发生/分录数）与按月展开预览
- 拼音检索：`core/pinyin.py` 内嵌常用汉字拼音表，`别名`/`会计科目` 解析新增拼音全拼与首字母匹配（前缀优先、多命中列候选），`别名 测试` 命令可查看解析结果
- 别名管理完善：`别名 列表 --全部` 查看内置别名
- 测试：预算进度与汇总、对账报告与标记写回、断言写入、拼音解析、预算/对账/定期/图表命令共 19 项

### Changed

- `AliasTable.resolve` 支持 `accounts` 参数以启用拼音检索；`resolve_account` 贯通 CLI、Web、TUI 三端
- 用户手册新增第 12 章「记账增强：预算 · 对账 · 定期 · 拼音」

## [0.4.0] - 2026-09-26

对应里程碑 M3：TUI 界面（终端三栏）。

### Added

- `界面`（`tui`）命令：自研 ANSI 三栏界面 `科目树 │ 流水 │ 详情`，无第三方库
- 终端层 `tui/term.py`（VT 启用、尺寸、raw/cooked 模式、按键解析）、`tui/screen.py`（行级差异重绘、宽字符占位）
- 控件：`widgets/list.py`（滚动列表、翻页、搜索）、`widgets/tree.py`（科目树、折叠展开、子科目小计）、`widgets/form.py`（记账表单、必填校验、cooked 输入）、`widgets/spark.py`（盲文迷你图，8 级）
- 键位：`a` 记账、`e` 编辑摘要、`/` 搜索、`f` 按科目筛选、`t` 支出排行、`s` 切换排序、`?` 帮助、`q` 退出；`Tab`/`Enter` 操作科目树与表单
- 表单记账复用 `core.actions`，与 CLI / Web 同一条写入路径；`e` 编辑走整块重写并保留元数据
- 测试：屏幕缓冲与差异重绘、盲文渲染、列表/树/表单控件、脚本化按键驱动完整记账与编辑流程、**TUI 代码量预算断言（≤ 800 行）**

### Changed

- 用户手册新增第 11 章「终端界面（TUI）」；版本规划的 TUI 模块清单去掉未使用的 `theme.py`（配色常量并入终端层，避免死代码）

## [0.3.0] - 2026-09-26

对应里程碑 M2：Web 界面（本地服务）。

### Added

- `服务`（`serve`）命令：`ThreadingHTTPServer` + 中文路由，默认监听 `127.0.0.1:5000`
- Web 数据层 `web/service.py`：按文件 mtime 缓存快照，写操作复用 `core.actions`，变更后自动失效
- HTTP 接口：`/api/概览`、`/api/流水`、`/api/余额`、`/api/科目`、`/api/别名`、`/api/待分类`、`/api/报表/<类型>`、`/api/图表/<类型>`、`/api/变更`（SSE）、`POST /api/记一笔`、`POST /api/归类`、`POST /api/导入预览`、`POST /api/导入`
- 静态前端（随包分发、无 CDN、无第三方库）：仪表盘、记一笔、流水、科目、报表、导入向导六页；ChartSpec 在前端渲染为 SVG，与 CLI/TUI 消费同一结构
- 三大财务报表：`report.balance_sheet`、`income_statement`、`cash_flow_statement`（现金流量按经营 / 投资 / 筹资分类）
- 待分类批量归类：`POST /api/归类` 整块改写交易并沉淀分类规则（走 `core.actions.reclassify`）
- 写操作内核 `core/actions.py`：CLI 与 Web 共用记账与归类逻辑（含收入方向符号推导）
- 安全约束：绑定非本机地址必须设置口令，否则拒绝启动；静态文件目录穿越防护

### Changed

- `writer.render_transaction` 支持渲染交易与分录元数据（归类等重写操作可无损保留 `; 键: 值`）
- 用户手册新增第 10 章「浏览器界面（本地服务）」与 HTTP 接口表

## [0.2.0] - 2026-09-26

对应里程碑 M1：报表、导入与语言完备。

### Added

- 语言完备：关键字中英双写（`选项/option`、`开立/open`、`断言/balance`、`定期/recur`、`起止/from-to`、周期与选项键值），`core/keywords.py` 为单一事实源
- 写回语言策略：默认中文、按文件跟随，`option "keyword_language"` 可强制
- `docs/语法大全.md`（对照表由 `scripts/生成语法大全.py` 从代码生成，测试断言一致）与 `docs/语法速查.md`
- `规范`（`normalize`）命令：全角 ↔ 半角一键规范化，含未闭合引号与不配对花括号的检测与 `--修复未闭合`
- `初始化`（`init`）命令：一键生成账本骨架（main.knot / 年份文件 / 别名 / 分类规则 / .gitignore）
- 一键运行：`knot.bat`（Windows）、`knot.sh`（macOS/Linux）自动准备环境并进入交互菜单；`scripts/打包.py` 生成 `dist/knot.pyz` 单文件
- `菜单`（`menu`）命令：交互式主菜单
- 报表 `报`（`report`）：概况 / 收支 / 净资产 / 分类 / 科目，支持 `--json`
- 图表 `图`（`chart`）：ChartSpec → 自包含 SVG，支持柱 / 线 / 饼 / 树（treemap）/ 热力（日历），>300 点自动降采样
- 账单导入 `导入`（`import`）：微信 / 支付宝（GBK 探测）/ 银行（单列或收支两列），表头定位、指纹去重、分类规则套用、`费用:待分类` 兜底、`--试运行`
- 测试：新增 `test_keywords`、`test_convert`、`test_init`、`test_report`、`test_importer` 与导入样例数据

### Changed

- 解析器接受中英关键字混写；`Options.from_pairs` 归一化中文选项键与取值
- 开发文档升至 1.2：新增 §3.8 关键字中英双写、§3.9 一键规范化、§16.2 M1 任务分解与功能 F11–F13
- 用户手册增补「一键运行与账本生成」「中英双写」「报表与图表」「账单导入」「一键规范化」五节

### Notes

- 诊断与提示文案保持中文（0.2.0 范围收敛；关键字纠错建议会按文件语言给出对应写法）
- PyPI 尚未发布，安装方式见 README

## [0.1.0] - 2026-09-26

对应里程碑 M0：内核与 CLI 记账。

### Added

- 仓库初始化：README、贡献指南（提交规范）、变更日志、GPL-3.0 许可证
- 开发文档《结绳Knot开发文档》纳入版本管理
- 版本规划文档 [《版本规划》](./docs/版本规划.md)：版本号自 `0.1.0` 起步，稳定化版本为 `0.7.0`
- 内核：中文基础模块（控制台 UTF-8、东亚宽度、全角归一化、中文数字金额、中文日期、别名系统、分类规则）
- 内核：词法与手写递归下降解析器，含恐慌模式恢复、中文诊断与纠错建议
- 内核：`include` 展开与解析缓存、定期交易展开、结构化查询过滤器
- 内核：校验流水线（自动开立科目、单腿配平、借贷平衡、库存成本、余额断言、会计恒等式）
- 内核：增量写回（行级编辑、原子替换、沿用既有缩进与列对齐）、格式化对齐、宽度感知表格
- CLI：`记 / 查 / 余 / 检查 / 整理 / 别名` 子命令，中文命令别名，`--补全` 生成 bash / zsh / fish 脚本
- `schema/GRAMMAR.md` 定稿；`schema/测试数据/` 黄金用例与 `tests/` 测试套件
- 用户手册 [docs/用户手册.md](./docs/用户手册.md)

### Changed

- 开发文档 v1.1：文件扩展名由 `.ledger` 统一为 `.knot`；Python 包版本起点由 `1.0.0` 调整为 `0.1.0`
